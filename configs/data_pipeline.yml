directories:
  raw: "/app/data/raw"
  processed: "/app/data/processed"
  manifests: "/app/data/manifests"

pipeline:
  seed: 42
  target_resolution: [224, 224]
  temporal_window: 16              

splits:
  train: 0.7
  val: 0.15
  test: 0.15